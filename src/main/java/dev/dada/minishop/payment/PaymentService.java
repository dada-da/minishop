package dev.dada.minishop.payment;

import org.springframework.stereotype.Service;

/**
 * TASK MS-20: createPayment(order) tra ve PENDING; confirmPayment(id) mock SUCCESS/FAILED.
 * Khi SUCCESS -> cap nhat Order.status = PAID.
 */
@Service
public class PaymentService {
    // TODO MS-20
}
